<h1> Index </h1>

<%= link_to 'Create a Job', new_job_path %>

<% @jobs.each do |job| %>
  <h2> <%= job.title %> </h2>
  <p> <%= job.company %> </p>
  <p> <%= link_to 'Show', job_path(job) %>
  <hr>
<% end %>
